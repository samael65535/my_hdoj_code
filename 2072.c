/* 统计不同的单词量 */
#include<string.h>
#include<stdio.h>
int main(int argc, char *argv[])
{
  char str[10000];
  char count[100][100];
  char c;
  while(gets(str)!=EOF){
    int i, j;
    char tmp[100];
    if(str[0]=='#') break;
    for(i=0; str[i]!='\n'; i++){
      if(str[i]==' '){ 
	for(j=0;j<;)
	  strcpy(cout[i],tmp);
      }
    }
  }
  return 0;
}
